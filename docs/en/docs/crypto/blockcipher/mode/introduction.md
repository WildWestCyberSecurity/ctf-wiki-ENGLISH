# Block Cipher Modes

Block ciphers divide the plaintext message into fixed-size blocks, and each plaintext block is encrypted into ciphertext under the control of a key. Of course, not every message is an exact multiple of the block size, so padding may be needed.
