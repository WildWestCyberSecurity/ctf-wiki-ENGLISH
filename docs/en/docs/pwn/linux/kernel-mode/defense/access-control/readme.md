# Introduction

Access control refers to the kernel adding access controls to certain objects, so that the corresponding objects in the kernel have certain access control requirements, such as being non-writable or non-readable.

