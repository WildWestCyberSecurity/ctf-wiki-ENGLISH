# DoS

Performing a DoS attack on the kernel is relatively easy. We can achieve this in the following ways:

- Trigger a vulnerability in the kernel to cause a kernel crash
- Trigger a deadlock in the kernel
- Trigger a large amount of kernel memory leaks, i.e., a large amount of memory is allocated but never freed
